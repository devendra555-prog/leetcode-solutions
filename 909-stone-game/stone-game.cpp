class Solution {
public:
    int sum(vector<int>& piles, int s, int l) {
        // yaha kaam karo
        return 0;
    }

    bool stoneGame(vector<int>& piles) {
        int n = piles.size();

        int s = 0;
        int l = n - 1;

        sum(piles, s, l);   // function call

        return true;
    }
};